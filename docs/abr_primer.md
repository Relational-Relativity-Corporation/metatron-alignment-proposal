# ABR Framework Primer

## Core Principle

Relation is mathematically primary. Nodes are derived.

The ABR operator framework computes relational structure directly
from observable data without assuming object identity, independence,
or stationarity. The conversation is the sensor data.

## Operator Composition

E(x, ρ) = R(B(A(x)), ρ(A(x)))

- **A** — difference operator: extracts relational contrast across topology
- **B** — accumulation operator: accumulates edges at forward vertices
- **R** — propagation operator: couples structure across topologies
- **C** — declared projection (application layer, not kernel)

## Three Declared Topologies (Conversation Domain)

- **T_P (Position):** sentence adjacency within turns
- **T_F (Feature):** declared adjacency on 8 measurable text properties
- **T_τ (Turn):** directed turn sequence — primary signal carrier

## Key Metrics

- **Γ:** R-sustained cross-topology circulation
- **P_Γ:** circulation-state decomposition across topologies
- **ΔP_Γ:** per-turn departure from declared relational baseline

## The Object Error

Index-local operators have a structural null space containing all
relational information present in coupled interactions. Divergence
from true relational structure is monotonically non-decreasing and
invisible to standard validation.

Full proof: arXiv:2601.22389

## V4 Topological Admissibility

Every application topology must be declared by Origin on the
observable index set after M. Two gates must pass:

- **Gate 1 — Evaluability:** topology must be declared on D
- **Gate 2 — Provenance:** topology must not import structure
  not present in the observable index set

Ring topology fails Gate 2 in application context and is
inadmissible outside the proof layer.